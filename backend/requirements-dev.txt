# Development / CI-only dependencies (not installed in the production image).
-r requirements.txt
pytest==8.3.3
pytest-cov==5.0.0
# TestClient needs httpx; keep it compatible with google-genai (>=0.28).
httpx>=0.28,<1.0
